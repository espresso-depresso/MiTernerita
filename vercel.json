{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/miternerita-front/browser"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "(.*)", "dest": "/index.html" }
  ]
}
